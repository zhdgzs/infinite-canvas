# 实施计划

1. 复用 Worker 的渠道与 Responses 输入逻辑，提供带 `stream: true` 的 OpenAI 文本请求。
2. 新增受认证的 SSE 转发路由，处理客户端断连、上游错误和反向代理缓冲禁用。
3. 在前端生成服务中实现 SSE 消费，并仅对 OpenAI 渠道改用该路径。
4. 更新生成任务契约、待测试记录和变更日志。
5. 进行差异与类型影响复核；遵循项目约定，不执行构建或测试。
